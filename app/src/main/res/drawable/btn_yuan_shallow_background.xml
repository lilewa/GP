<?xml version="1.0" encoding="utf-8"?>

    <shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 填充颜色 主题色 -->
    <solid android:color="@color/app_btn_shallow_backgroundbankgrad"/>
    <!-- 线的宽度，颜色灰白-->
    <stroke android:color="@color/app_btn_shallow_backgroundbankgrad" android:width="0.5dp"/>
    <!-- 矩形的圆角半径 -->
    <corners android:radius="5dp"/> </shape>